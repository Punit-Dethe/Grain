[package]
name = "rolling-window"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
readme = "README.md"
description = "Pure rolling-window cursor, timing-aware transcript assembler, and bounded lexical seam reconciliation."

[dependencies]
strsim = "0.11"

[dev-dependencies]
