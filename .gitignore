# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

package-lock.json

node_modules
dist
dist-ssr
*.local
*.local.*

# Editor directories and files
.vscode/
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agents / IDEs
.claude/
.cursor/
.gemini/
.coder/

/target/
recording_*
.crush/

# Playwright
playwright-report/
test-results/
blob-report/

.direnv
.envrc

# Nix build output
result

# Grain workspace artifacts
/target
/models/

# [GRAIN] machine-local build config (short target-dir for whisper MAX_PATH)
src-tauri/.cargo/
# Added by code-review-graph
.code-review-graph/

# [GRAIN] Updater signing keys — NEVER commit these. The real keypair lives
# outside the repo (~/.grain-keys); these patterns are a safety net.
.grain-keys/
*.updater.key
*.updater.key.pub
*.updater.key.password
grain.updater.key*
