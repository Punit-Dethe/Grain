{
  "note": "Files still shared with upstream under src/. The census reached ZERO at the UI 2.0 cutover (frontend moved to src/app/, which upstream does not use), so strict is on: any frontend change during a sync is now a hard failure. See docs/UI 2.0/PLAN.md.",
  "strict": true,
  "shared": []
}
