# Handy intake — 2026-09-23

This is a triage snapshot, not a merge verdict. The authoritative per-commit
decisions are in `verdicts.json`; re-run `preflight.py` before syncing because
`upstream/main` can advance. Grain is 42 commits behind `8f9cf53c` at this
snapshot. A trial merge reports 24 conflicts, including frozen frontend paths.

The first low-risk adaptations are on `sync/upstream-2026-09-23`:
`496a80cc` (History copy feedback) and `7c31572d` (settings scroll reset).
`286e66cb`, `330b2ddb`, and `1cd92662` have frontend review records with
evidence. These adaptations do not advance upstream ancestry.

| Commit | Intake lane | What to assess before a verdict |
| --- | --- | --- |
| `286e66cb` | Reviewed | Generated bindings whitespace; Grain's generated API is owned here. |
| `5ec2276a` | High | Single-writer tray icon touches actions, Secure Input, shortcuts, tray, and shell. Preserve Grain's native pill and branded tray model. |
| `f6fac42e` | Hold | Updater dependency bump. Reconcile the runbook's updater policy with the current Grain updater code before taking it. |
| `8fd66912` | Low | Nix lock/build repair; compare Grain's generated lock derivatives. |
| `af48dd68` | Low | v0.9.6 release metadata and versioning; keep Grain identity. |
| `df216832` | High | Tail-audio drain changes recorder, resampler, and VAD; preserve Grain's existing resampler tail fix and capture contract. |
| `20ada47d` | High | Experimental Earshot VAD adds dependencies, settings, backend selection, and UI; requires a separate product decision. |
| `258899a2` | Medium | Production log redaction; check Grain's parallel transcription and action logging. |
| `c7a68fed` | Medium | GNOME Wayland clipboard paste should skip wtype; verify Grain's Linux paste route. |
| `fe5b23c4` | Medium | External paste scripts should not block the UI/recording path; verify Linux behavior. |
| `c6fa60da` | High | Toggle parity during in-flight transcription changes coordinator and signal handling. |
| `b0266607` | Hold | Nix update-check policy crosses settings, shell, and frontend; reconcile Grain's current updater behavior first. |
| `6fa85061` | Medium | Short word “no” lost by language filtering; inspect model/transcription policy and Grain's language UI. |
| `c62a5fcd` | High | Auto push-to-talk changes shortcut semantics and settings across frontend/backend. |
| `00d25549` | Medium | Secure Input reconciliation must preserve held shortcuts; macOS-specific verification. |
| `fbd4e15f` | Medium | Launching as an accessory affects macOS window and dock behavior. |
| `bc7facea` | Low review | 5000 ms paste-delay debug control; assess whether Grain exposes or needs it. |
| `330b2ddb` | Reviewed | Mac scrollbar styling issue is already covered by Grain's scoped styles. |
| `988521f8` | Low review | Debug/onboarding preview UI; inspect for useful diagnostics without merging Handy UI. |
| `1cd92662` | Reviewed | Onboarding scrollbar overflow is already covered by Grain's fixed inset shell. |
| `e29520d6` | Medium review | Windows overlay text scaling; compare against Grain's native pill rather than porting web overlay CSS. |
| `d54c88eb` | High | Real-time-safe microphone callback changes capture internals and vendored dependencies. |
| `42afd1bf` | Low review | Stale clipboard paste troubleshooting copy; adapt only relevant Grain documentation. |
| `e4ae0d44` | Medium | Recover when model files are deleted externally; check model manager, transcription manager, and Grain UI. |
| `2c5c7601` | Medium | Windows Vulkan loader environment default; verify GPU behavior and placement before engine setup. |
| `ed1bfb9b` | Low review | Catalan locale; manually adapt to Grain's frontend schema if supported. |
| `14cfed8a` | Low | Windows CI fix; compare Grain's owned workflow. |
| `2bdf9ac0` | Low review | Indonesian locale; manually adapt to Grain's frontend schema if supported. |
| `bf8756c3` | Medium | Disable WebView2 browser accelerator keys in the main window; test Grain keyboard shortcuts. |
| `db1aaac7` | High | transcribe.cpp 0.2.3 baseline/ABI; Grain already declares 0.2.3, so audit vendor equality and ancestry before any merge. |
| `ba10ce19` | Low review | Fedora/Ubuntu install documentation. |
| `53c55d70` | Low review | README slimming; retain Grain's documentation structure. |
| `53766a27` | Low review | README edit; ledger's subject-only “already applied” hint needs blob verification. |
| `055c7a5e` | Low review | README edit; ledger's subject-only “already applied” hint needs blob verification. |
| `69175114` | Low review | Sponsor copy/image; Grain branding decision. |
| `05e0aedd` | Low | v0.9.7 release metadata and versioning; keep Grain identity. |
| `dc5bdc9d` | Medium | Unload model after no-audio recordings; check Grain batch, rolling, and Flow lifecycles. |
| `7c31572d` | Adapted | Reset settings scroll on section change in Grain-owned SettingsPage. |
| `a6eed754` | Low, in progress | Unknown shortcut reset IDs should return an error rather than panic; port to compiled Grain settings and upstream-shaped shortcut command. |
| `496a80cc` | Adapted | Show History copy success only after clipboard write resolves. |
| `141f981d` | Medium | Compound shortcut key names must be parseable; review Rust-to-frontend keyboard representation. |
| `8f9cf53c` | Low review | Omarchy recording-indicator documentation; confirm behavior with Grain's native indicator. |

The pre-existing divergence ratchet currently reports growth in ten
Handy-derived or shell files and shrinkage in two others. Do not accept the
growth budget without reviewing each file. The focused Rust test cannot yet run
on this Windows checkout because the vendored transcribe.cpp CMake build fails;
frontend build, TypeScript, and targeted ESLint passed for the completed ports.
